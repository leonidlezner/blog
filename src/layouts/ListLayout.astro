---
import Layout from "./Layout.astro";
const { title, area } = Astro.props;
---

<Layout title={title} area={area}>
  <div class="container mx-auto max-w-3xl my-10">
    <slot />
  </div>
</Layout>
