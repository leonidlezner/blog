---
import Categories from "./Categories.astro";
import DateString from "./DateString.astro";
import Newspaper from "astro-heroicons/outline/Newspaper.astro";

interface Props {
  author: string;
  pubDate: string | undefined;
  categories: string[];
}

const { author, categories, pubDate } = Astro.props;
---

<div class="mb-7 text-gray-600 flex space-x-1 flex-wrap items-center">
  <Newspaper class="w-5 h-5" />
  <div>Gepostet von <span class="font-semibold">{author}</span></div>
  {
    categories && (
      <div>
        in
        <Categories categories={categories} />
      </div>
    )
  }
  {pubDate && <DateString date={new Date(pubDate)} />}
</div>
